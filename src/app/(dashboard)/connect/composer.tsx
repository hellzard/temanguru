"use client";
import { Check, Copy, ExternalLink } from "lucide-react";import { useState } from "react";
export function QuickComposer(){const[phone,setPhone]=useState('');const[message,setMessage]=useState('Selamat pagi, Bapak/Ibu. Kami ingin menyampaikan perkembangan belajar putra/putri Anda.');const[copied,setCopied]=useState(false);const normalized=phone.replace(/\D/g,'').replace(/^0/,'62');async function copy(){await navigator.clipboard.writeText(message);setCopied(true);setTimeout(()=>setCopied(false),1500)}return <section className="tg-card p-5"><h2 className="font-[family-name:var(--font-display)] text-lg font-extrabold tracking-[-0.035em]">Kirim tanpa menyimpan nomor</h2><p className="mt-1 text-sm tg-muted">Nomor hanya dipakai di browser untuk membuka aplikasi pesan dan tidak disimpan ke database.</p><div className="mt-5 space-y-4"><label className="block text-sm font-bold">Nomor WhatsApp<input value={phone} onChange={event=>setPhone(event.target.value)} inputMode="tel" placeholder="08…" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Pesan<textarea value={message} onChange={event=>setMessage(event.target.value)} rows={7} maxLength={4000} className="mt-2 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] p-3"/></label><div className="flex flex-wrap gap-2"><button type="button" onClick={()=>void copy()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-[var(--tg-border)] px-4 font-bold">{copied?<Check size={17}/>:<Copy size={17}/>}Salin</button><a href={normalized?`https://wa.me/${normalized}?text=${encodeURIComponent(message)}`:'#'} target="_blank" rel="noreferrer" aria-disabled={!normalized} className={`tg-primary-button ${!normalized?'pointer-events-none opacity-50':''}`}><ExternalLink size={17}/>Buka WhatsApp</a></div></div></section>}
