import { Award, FolderHeart } from "lucide-react";import { EmptyState } from "@/components/dashboard/empty-state";import { FormMessage } from "@/components/dashboard/form-message";import { PageHeader } from "@/components/dashboard/page-header";import { SubmitButton } from "@/components/dashboard/submit-button";import { firstParam } from "@/lib/action-result";import { formatDate } from "@/lib/format";import { requireActiveSchool } from "@/lib/schools/active-school";import { createClient } from "@/lib/supabase/server";import { createStudentPortfolio,createTeacherPortfolio } from "./actions";
export const metadata={title:'Portofolio'};function studentName(v:unknown){if(Array.isArray(v))return studentName(v[0]);if(v&&typeof v==='object'&&'display_name'in v)return String((v as Record<string,unknown>).display_name);return'Murid'}
export default async function PortfoliosPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const params=await searchParams;const context=await requireActiveSchool();const supabase=await createClient();const[students,teacherItems,studentItems]=await Promise.all([supabase.from('students').select('id,display_name').eq('school_id',context.active.schoolId).eq('status','active').order('display_name'),supabase.from('portfolios_teacher').select('id,title,description,category,achieved_on,evidence_url').eq('school_id',context.active.schoolId).order('created_at',{ascending:false}),supabase.from('portfolios_student').select('id,title,description,evidence_date,evidence_url,students(display_name)').eq('school_id',context.active.schoolId).order('created_at',{ascending:false})]);const error=[students,teacherItems,studentItems].find(v=>v.error)?.error;if(error)throw error;return <div><PageHeader title="Portofolio" description="Kumpulkan bukti karya murid dan pengembangan profesional guru."/><FormMessage error={firstParam(params.error)} success={firstParam(params.success)}/><div className="mt-7 grid gap-6 xl:grid-cols-2"><section className="tg-card p-5"><h2 className="font-[family-name:var(--font-display)] text-lg font-extrabold tracking-[-0.035em]">Portofolio guru</h2><form action={createTeacherPortfolio} className="mt-5 space-y-4"><label className="block text-sm font-bold">Judul<input name="title" required className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Kategori<select name="category" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"><option value="achievement">Prestasi</option><option value="training">Pelatihan</option><option value="project">Proyek</option><option value="publication">Publikasi</option></select></label><label className="block text-sm font-bold">Tanggal<input type="date" name="date" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Tautan bukti<input type="url" name="evidence_url" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Deskripsi<textarea name="description" rows={4} className="mt-2 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] p-3"/></label><SubmitButton>Tambah</SubmitButton></form></section><section className="tg-card p-5"><h2 className="font-[family-name:var(--font-display)] text-lg font-extrabold tracking-[-0.035em]">Portofolio murid</h2><form action={createStudentPortfolio} className="mt-5 space-y-4"><label className="block text-sm font-bold">Murid<select name="student_id" required className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"><option value="">Pilih</option>{((students.data??[]) as Array<Record<string,unknown>>).map(student=><option key={String(student.id)} value={String(student.id)}>{String(student.display_name)}</option>)}</select></label><label className="block text-sm font-bold">Judul<input name="title" required className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Tanggal<input type="date" name="date" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Tautan bukti<input type="url" name="evidence_url" className="mt-2 min-h-11 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] px-3"/></label><label className="block text-sm font-bold">Deskripsi<textarea name="description" rows={4} className="mt-2 w-full rounded-xl border border-[var(--tg-border)] bg-[var(--tg-surface)] p-3"/></label><SubmitButton>Tambah</SubmitButton></form></section></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><section><h2 className="mb-3 text-lg font-[family-name:var(--font-display)] text-lg font-extrabold tracking-[-0.035em]">Bukti profesional</h2>{teacherItems.data?.length?<div className="space-y-3">{((teacherItems.data??[]) as Array<Record<string,unknown>>).map(item=><article key={String(item.id)} className="tg-card p-5"><div className="flex gap-3"><Award className="text-[var(--tg-primary)]" size={21}/><div><h3 className="font-[family-name:var(--font-display)] font-extrabold tracking-[-0.03em]">{String(item.title)}</h3><p className="text-sm tg-muted">{String(item.category)} · {formatDate(item.achieved_on?String(item.achieved_on):null)}</p></div></div></article>)}</div>:<EmptyState icon={Award} title="Belum ada bukti guru" description="Tambahkan pelatihan, prestasi, proyek, atau publikasi."/>}</section><section><h2 className="mb-3 text-lg font-[family-name:var(--font-display)] text-lg font-extrabold tracking-[-0.035em]">Karya murid</h2>{studentItems.data?.length?<div className="space-y-3">{((studentItems.data??[]) as Array<Record<string,unknown>>).map(item=><article key={String(item.id)} className="tg-card p-5"><div className="flex gap-3"><FolderHeart className="text-[var(--tg-primary)]" size={21}/><div><h3 className="font-[family-name:var(--font-display)] font-extrabold tracking-[-0.03em]">{String(item.title)}</h3><p className="text-sm tg-muted">{studentName(item.students)} · {formatDate(item.evidence_date?String(item.evidence_date):null)}</p></div></div></article>)}</div>:<EmptyState icon={FolderHeart} title="Belum ada karya murid" description="Simpan bukti proyek, karya, atau pencapaian murid."/>}</section></div></div>}
