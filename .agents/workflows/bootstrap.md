# Bootstrap Teman Guru

Description: Validate the hybrid starter, inspect the existing GitHub repository, and prepare the first production-quality vertical slice.

1. Read `START_HERE.md`, `STARTER_CODE_SCOPE.md`, `AGENTS.md`, `ANTIGRAVITY_EXECUTION_BRIEF.md`, and `docs/AGENT_OPERATING_MODEL.md`.
2. Run `npm run verify:workspace`.
3. Inspect package versions against official documentation and security releases before changing them.
4. Run install, secret scan, lint, typecheck, unit tests, and production build when the environment allows.
5. Inspect current Git status, branch, remotes, and remote `hellzard/temanguru`.
6. Preserve working starter code and repository history.
7. Review Supabase migrations and RLS with backend/security agents.
8. Verify landing, login fallback, onboarding code, dashboard shell, offline page, and `/api/health`.
9. Update `docs/IMPLEMENTATION_STATUS.md` with verified facts only.
10. Plan the earliest incomplete vertical slice. Do not build multiple business modules at once.
