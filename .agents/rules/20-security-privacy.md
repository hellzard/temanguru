# Security and Privacy Rule

Never expose secrets, service-role credentials, or identifiable student data. Every mutation verifies authentication, tenant membership, role, input shape, and ownership. RLS is mandatory. Private data must not enter analytics, URLs, console logs, cache keys, generated filenames, or AI prompts.
