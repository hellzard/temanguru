# AI Feature Gate

Activation: Always On

AI generative features are disabled. Do not add AI SDKs, provider keys, AI buttons, AI copy, embeddings, or inference endpoints until the user explicitly opens the AI gate. Core functionality must work without AI.
