# Dependency Safety Rule

Prefer platform APIs and existing dependencies. Before adding a package, verify its official repository/package identity, maintenance, license, security posture, bundle impact, and necessity. Pin a reviewed version through the lockfile. Never install a package based only on an AI-suggested name.
