-- Development-only seed. Replace UUIDs after creating local auth users.
-- Never run against production without reviewing every row.

-- Example:
-- select public.create_school_with_owner('SMP Contoh', 'Asia/Jakarta');
