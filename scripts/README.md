# Scripts

Run from workspace root:

```bash
node scripts/verify-agent-workspace.mjs
```

This only validates the instruction package. After Antigravity scaffolds the application, the agent must create the project's real lint, typecheck, test, build, secret-scan, and release-preflight commands.
