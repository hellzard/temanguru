# Teman Guru Workspace Context

Read `AGENTS.md` and `ANTIGRAVITY_EXECUTION_BRIEF.md` first.

This workspace contains product specifications and agent customizations, not the implementation. Build the application from scratch while preserving:

- repository `hellzard/temanguru`;
- Vercel project `temanguru`;
- production URL `https://temanguru.vercel.app`;
- Indonesian language;
- AI disabled;
- privacy-first and mobile-first behavior.

Use the workspace skills and custom agents. Run `/bootstrap` before implementation.
